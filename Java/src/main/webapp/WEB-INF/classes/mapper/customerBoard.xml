<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="customerBoard">	
	<insert id="add" parameterType="article" useGeneratedKeys="true" keyProperty="id">
		/* customerBoard.add */
		INSERT INTO `customer_board` 
		(title, content, create_date, modify_date, userid, username, notice, company) 
		VALUES (#{title}, #{content}, now(), now(), #{userid}, #{username}, #{notice}, #{company}) 
	</insert>	
</mapper>